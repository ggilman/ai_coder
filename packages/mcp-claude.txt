# ============================================================
# mcp-claude.txt — extra MCP servers for the Claude workbench only
#
# Format (pipe-delimited, one server per line):
#   npm-package | server-key | command | arg1 arg2 ... | ENV_VAR1,ENV_VAR2 | net
#
# Set 6th field to "online" to skip the server when network isolation is active.
#
# Use {workspace} as a placeholder for the container workspace path.
# Lines starting with # are ignored. Blank lines are ignored.
# ============================================================

# OpenSCAD — create, validate, and render .scad files directly from the agent.
# Tools: render to STL/PNG, validate syntax, list variables/modules, export.
# Not on PyPI; installed from source. Requires openscad + xvfb (both in apt-claude.txt).
pip:git+https://github.com/quellant/openscad-mcp.git | openscad | openscad-mcp | |
